# FadilJaya.NET — users file (static fallback)
# Tidak dipakai untuk user biasa — semua user diambil dari MySQL.
# File ini hanya untuk fallback dan testing lokal.

# DEFAULT  Framed-Protocol == PPP
#     Framed-Protocol = PPP,
#     Framed-Compression = Van-Jacobson-TCP-IP

DEFAULT Auth-Type := Reject
    Reply-Message = "Akses ditolak"
