# detail — log all accounting packets to flat file (useful for debugging)
detail {
    filename    = ${radacctdir}/%{Client-IP-Address}/detail-%Y%m%d
    header      = "%t"
    permissions = 0600
    locking     = no
    log_packet_header = no
}
